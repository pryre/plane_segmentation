geometry_msgs/Vector3 normal    # Plane normal (nx, ny, nz).
geometry_msgs/Point center      # Plane 3D center (cx, cy, cz).

uint64 image_x                  # Plane 2D center row on the image
uint64 image_y                  # Plane 2D center column on the image
uint32 mask_color     # BGRx. Plane mask color (blue, green, red, [unused]).

